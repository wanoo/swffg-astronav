node_modules/
dist/
.DS_Store
package.json
package-lock.json

# sources régénérables du pack (via build_pack.py depuis data/planets.json)
packs/_source/
packs/_macros_src/

# fichiers debug LevelDB (recréés à l'ouverture)
packs/*/LOG
packs/*/LOG.old
packs/*/LOCK
